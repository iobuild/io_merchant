
<div class="container-fluid">
  <div class="row">
    <div class="col-md-4">
      <ul>
        <% @cart.cart_items.each do |item| %>
          <li>
            <%= item.sellable.title %>
            <%= item.sellable.price %>
            <%= item.quantity %>
            <%=  %>
          </li>
        <% end %>

      </ul>


      <%= @cart.amount %>
    </div>

    


  </div>
</div>
